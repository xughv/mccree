import { call, take, put, select, throttle } from 'redux-saga/effects';

// const delay = (time) => new Promise(resolve => setTimeout(resolve, time));

export {
  call,
  take,
  put,
  select,
  throttle,
}